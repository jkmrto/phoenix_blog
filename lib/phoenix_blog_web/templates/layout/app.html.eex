<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PhoenixBlog · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>

<body>
    <header id="header">
        <section class="container">
            <div id="nav-bar" class="flex-container row nav-bar">
                <div class="nav-link"> <%= link "Index", to: Routes.post_path(@conn, :index) %> </div>
                <div class="nav-link"> <%= link "Contact", to: Routes.contact_path(@conn, :index) %> </div>
                <div class="nav-link"> <%= link "Resume", to: Routes.resume_path(@conn, :index) %> </div>
            </div>
        </section>
    </header>
    <main role="main" id="main-container" class="container">
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
        <%= @inner_content %>
    </main>
</body>

</html>
