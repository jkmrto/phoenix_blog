<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title(assigns) %>  </title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
    <!-- Global site tag (gtag.js) - Google Analytics -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-0T4NY8QTPE"></script>
    <script>
      window.dataLayer = window.dataLayer || [];

      function gtag() {
        dataLayer.push(arguments);
      }
      gtag("js", new Date());

      gtag("config", "G-0T4NY8QTPE");
    </script>
  </head>

  <body>
    <header id="header">
      <section class="container">
        <div id="nav-bar" class="d-flex flex-row justify-content-end w-100 px-5">
          <div class="nav-link"><%= link "Landing", to: Routes.landing_path(@conn, :index) %></div>
          <div class="nav-link"><%= link "Posts", to: Routes.post_path(@conn, :index) %></div>
          <div class="nav-link"><%= link "Resume", to: Routes.resume_path(@conn, :index) %></div>
          <div class="nav-link"><%= link "Contact", to: Routes.contact_path(@conn, :index) %></div>
        </div>
      </section>
    </header>
    <main role="main" id="main-container" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
