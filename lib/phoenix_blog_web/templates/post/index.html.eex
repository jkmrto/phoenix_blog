<ul  >
  <%= Enum.map(@posts, fn(post) -> %>
    <li style="display: flex; justify-content: space-between">
      <%= link post.title, to: Routes.post_path(@conn, :show, post.slug) %>
      <span> <%=  post.date %> </span>
    </li>
  <% end) %>
</ul>
